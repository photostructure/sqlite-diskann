{
  "name": "sqlite-diskann-benchmarks",
  "version": "0.1.0",
  "description": "Performance benchmarks comparing sqlite-diskann vs sqlite-vec",
  "type": "module",
  "private": true,
  "scripts": {
    "prepare": "tsx scripts/prepare-datasets.ts",
    "bench": "tsx scripts/run-benchmark.ts",
    "bench:quick": "npm run bench profiles/quick.json",
    "bench:medium": "npm run bench profiles/medium.json",
    "bench:standard": "npm run bench profiles/standard.json",
    "bench:recall": "npm run bench profiles/recall-sweep.json"
  },
  "dependencies": {
    "cli-table3": "^0.6.5",
    "@photostructure/sqlite-vec": "^1.0.0",
    "usearch": "^2.21.4"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
